import java.util.*; fun main(){      val scanner =Scanner(System.`in`);      val (n,k)=readLine()!!.split(" ").map{it.toInt()}       val arr = readLine()!!.split(" ").map { it.toInt() }.toIntArray()       var ans=0;        for(i in 0 until n)              {            if(arr[i]>=arr[k-1]&&arr[i]>0)            {                  ans++            }                   }       println(ans); }