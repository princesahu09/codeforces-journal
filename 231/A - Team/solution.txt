package mainimport "fmt"func main(){ 	var problem int	fmt.Scan(&problem)	var ans=0 	for i := 0; i < problem; i++ {		var a ,b ,c int		fmt.Scan(&a,&b,&c) 		if a+b+c>=2{			ans++		}			} 	fmt.Println(ans)	}