import java.util.*;public class program1834A{      public  static void main(String[] args)      {            Scanner sc=new Scanner(System.in);            int testcase=sc.nextInt();            while(testcase-- >0)            {                  int size=sc.nextInt();                  int[] arr=new int[size];                  int plus=0;                  int minus=0;                    for(int i=0;i<size;i++)                  {                        arr[i]=sc.nextInt();                        if(arr[i]==1)                        {                              plus++;                        }                        else{                              minus++;                        }                  }                  int flip=0;                  int sum=plus-minus;                  if(minus%2==1)                  {                        sum+=2;                        flip++;                  }                  if(sum<0)                  {                       int need = (-sum+1)/2;                       flip+=need;                       if(need%2==1)                       {                        flip++;                       }                    }                  System.out.println(flip);                                                                                   }       }}