#include <bits/stdc++.h>using namespace std;int solve(vector<int> &nums, int n){            return *max_element(nums.begin(),nums.end());} int main(){      int t;      cin >> t;      while (t-- > 0)       {            int n;            cin >> n;             vector<int> nums(n, 0);            for (int i = 0; i < n; i++)            {                  int temp;                  cin >> temp;                  nums[i] = temp;            }             cout << solve(nums, n) << endl;      }       return 0;}