#include <iostream>#include <vector>#include<algorithm>using namespace std; int main(){      int testcase;      cin >> testcase;      while (testcase > 0)      {             int size;            cin >> size;             vector<int> arr(size);            for (int i = 0; i < size; i++)            {                 cin>>arr[i];            }             int x;            cin >> x;             long long low=*min_element(arr.begin(),arr.end());            long long high=* max_element(arr.begin(),arr.end());             if(x>=low&&x<=high)            {                  cout<<"YES"<<endl;            }            else{                  cout<<"NO"<<endl;            }                                     testcase--;      }       return 0;}