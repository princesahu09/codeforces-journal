import java.util.* fun solve(arr: IntArray, n: Int): Int {     var ans = 0    val freq = HashMap<Int, Int>()     for (num in arr) {        freq[num] = freq.getOrDefault(num, 0) + 1    }     for (entry in freq.values) {        ans += entry / 2    }     return ans} fun main() {    val scanner = Scanner(System.`in`)    val t = scanner.nextInt()     repeat(t) {        val n = scanner.nextInt()        val arr = IntArray(n)         for (i in 0 until n) {            arr[i] = scanner.nextInt()        }         println(solve(arr, n))    }}